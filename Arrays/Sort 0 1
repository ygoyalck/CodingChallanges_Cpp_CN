// arr - input array
// n - size of array

void SortZeroesAndOne(int arr[], int n){
 int z=0;
  int o=0;
for(int i=0;i<n;i++)
{
    if(arr[i]==0)
      z++;
  else
    o++;
}
  for(int i=0;i<z;i++)
    arr[i]=0;
  for(int i=z;i<z+o;i++)
    arr[i]=1;

}
