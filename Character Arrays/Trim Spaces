#include<bits/stdc++.h>
void trimSpaces(char input[]) {
  if(strlen(input)==0)
    return;
  trimSpaces(input+1);
  if(input[0]==' ')
   { for(int i=0;i<strlen(input);i++)
    {
      input[i]=input[i+1];
    }
}
}
