#include<iostream>
using namespace std;

int main() {
	int n;
  cin>>n;
  int se=0,so=0;
  while(n>0)
  {
    if((n%10)%2==0)
    {
      se+=n%10;
    }
    else
    {
      so+=n%10;
    }
    n/=10;
  }
  cout<<se<<"	"<<so;
}
